0.0.1 : Initial release,

0.0.2 :
- can call function to assign to variable and in if command,
- correct bug in GetValueOfTStrings don't use directely Variables.isSet,
- correct bug in DeleteVirguleAndParenthese to not delete parenthese when $var=(1+2) * (2+3),
- correct bug in for commande,
- convert pointer into class,
- make all array functions,
- add inline if support "xx ? yyy : zzz",
- add global command,
- add multi argument (fixed or optional) in function,
- new class TStringInteger for manage label and user function,
- now we have only one file for variables,
- support of \n \r \t \0 in string,
- add isHexa,
- make error if function found before endfunc,
- port to lazarus (Now, write once, compile everywhere !),
- replace operator # and @ by function chr and ord,
- add extension support,
- correct lot of bug,

0.0.2a :
- correct bug in DeleteVirguleAndParenthese function,
- add eval and call command,
- replace TStringList by my TLiteString (no better performance but i don't want
  not necesary code),
- correct bug in GetValueOfString, infinity loop when - is used,
- detect if multi-point or x in number,
- add GlobalQuit to force exit in eval command,
- add -params support to paste argument to main script,
- correct bug when @ using to don't show message,
- add @ in documentation,
- add echo and die command,
- correct bug in + operator,
- correct bug in <>, <=, >=, <, >, = operator when is number,

0.0.3 :
- add strCopy, strLowerCase, strPos, strReplace, strTrim, strTrimLeft,
  strTrimRight, srtUpperCase, strExplode, strImplode, strLoadFromFile, 
  strSaveToFile, strAddSlashes, strDeleteSlashes, strPrintR,
- add ExtractIntPart, ExtractFloatPart, DecToHex, DecToOct, DecToBin, Pi, uniqId,
- add header, getcookie, getget, getcookienumber, getgetnumber, getenv, setcookie,
- add Get, Cookie, Post (multipart/form-data not support for the moment) Support,
- add urlencode, urldecode,
- rewrite explode function,
- correct bug when error in loadcode,
- correct bug when error in isHexa,
- correct bug in TVariables.CreateArray function,
- add SaveToFile in TLiteStrings,
- correct bug in isInteger with hexa number,
- correct bug in TInternalFunction.Give (it was casesensitive),
- correct bug in MyStrToFloat,
- replace TLiteString by TStringList cause my object work only on windows platform,

0.0.4 :
- correct bug to set element of string,
- correct GetValueOfString to correct value when string used,
- add ini configuration,
- correct bug in strReplace command,
- support of file transfert,
- correct bug in RealPath function,

0.0.5 :
- add start index in strpos,
- add soundex, ucfirst, ucwords, strireplace, stripos, strrpos, strripos, strtr,
  strreplaceaccent, strcspn, strspn, arrayrsort, strrev, outputbufferstart,
  outputbufferclear, outputbufferwrite, outputbufferstop, outputbufferget, crc32,
  sleep, nltobr, bintohex, rol, ror, md5,
- correct bug in GetValueOfStrings function,
- add //, /*, (* comment support and delete {} comment for possible evolution,
- code can be integrated in HTML code,
- correct and optimise call user function,

0.1.0 :
- add max, min, bintodec, exp, ln, cos, acos, acosh, sin, asin, asinh, tan, atan,
  atan2, atanh, abs, frac, cot, acot, int, round, sqrt, sqr, trunc, rand, ceil,
  ensurerange, frexp, inrange, ldexp, lnxp1, log10, log2, logn, poly, samevalue,
  roundto,
- correct bug when non-blank char after comma,
- add Init in Module to get DocRoot, TmpDir en SafeMode,